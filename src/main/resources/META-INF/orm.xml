<?xml version="1.0" encoding="UTF-8"?>
<entity-mappings xmlns="https://jakarta.ee/xml/ns/persistence/orm"
                 xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                 xsi:schemaLocation="https://jakarta.ee/xml/ns/persistence/orm https://jakarta.ee/xml/ns/persistence/orm/orm_3_0.xsd"
                 version="3.0">

    <!--
      Default entity listeners apply to EVERY entity in the persistence unit.
      TenantEntityListener stamps wsTenantName from TenantContext on insert (when null),
      so tenant-scoped entities never fail their NOT NULL constraint. See the listener's
      javadoc for the safety guarantees.
    -->
    <persistence-unit-metadata>
        <persistence-unit-defaults>
            <entity-listeners>
                <entity-listener class="com.ws.wsAgenticSecurityGateway.common.listener.TenantEntityListener"/>
            </entity-listeners>
        </persistence-unit-defaults>
    </persistence-unit-metadata>

</entity-mappings>
